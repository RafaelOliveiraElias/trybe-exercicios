const random100 = () => Math.floor(Math.random() * 101) 

const caixaAlta = (string) => string.toUpperCase();

const firstLetter = (string) => string[0];

const concat = (s1, s2) => s1 + s2;


module.exports = { random100, caixaAlta, firstLetter, concat};
